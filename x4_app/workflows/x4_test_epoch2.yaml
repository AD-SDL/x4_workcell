name: Test_Epoch2
metadata:
  author: Ryan D. Lewis
  info: Runs a plate read on the experiment
  version: 0.2

modules:
  - name: epoch2

flowdef:
  - name: Run Epoch2 Experiment
    module: epoch2
    action: run_experiment
    args:
      experiment_file_path: payload.experiment_file_path
    comment: This will run the specified experiment file on the Epoch 2 in the glovebox
